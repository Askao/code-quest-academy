{
  "track": "gcse",
  "topic": "files",
  "lessons": [
    {
      "slug": "gcse-files-1",
      "title": "Writing and reading a file",
      "summary": "open(), write(), read() and close() - and why write mode starts empty.",
      "notes": "```python\nfile = open(\"notes.txt\", \"w\")   # \"w\" = write (creates the file, or empties it if it exists)\nfile.write(\"hello\\n\")\nfile.close()\n\nfile = open(\"notes.txt\", \"r\")   # \"r\" = read (this is the default mode)\ntext = file.read()\nfile.close()\nprint(text)\n```\n\n---\n\nAlways call `.close()` on a file after you're done with it — this makes sure everything you wrote is actually saved.\n\n`\"w\"` mode starts the file **completely empty** every time you open it that way, even if it already had content — any old content is lost. To add to the end instead of replacing everything, use `\"a\"` (append) mode instead.\n\n`.write()` does **not** add a newline for you — include `\\n` yourself whenever you want one.",
      "worked_example": "line = input()\nfile = open(\"notes.txt\", \"w\")\nfile.write(line)\nfile.close()\n\nfile = open(\"notes.txt\", \"r\")\nprint(file.read())\nfile.close()",
      "worked_example_note": "The file is written to and fully closed before it's reopened for reading - you write and close, then separately open and read.",
      "worked_example_demo_input": ["Remember to revise Python"]
    },
    {
      "slug": "gcse-files-2",
      "title": "Processing file contents",
      "summary": "Reading line by line, stripping newlines, and the safer with block.",
      "notes": "You can loop through a file's lines directly:\n\n```python\nfile = open(\"scores.txt\", \"r\")\nfor line in file:\n    print(line.strip())   # strip() removes the trailing newline\nfile.close()\n```\n\nEach line read from a file keeps its trailing newline character attached, even the last one might not — `.strip()` removes it, which matters before comparing text or converting a line to a number.\n\n---\n\n`with open(...) as file:` is a shorter, safer way to open a file: it closes the file automatically once the indented block finishes, even if something goes wrong partway through.\n\n```python\nwith open(\"scores.txt\", \"r\") as file:\n    for line in file:\n        print(line.strip())\n```",
      "worked_example": "lines = [input() for _ in range(3)]\nwith open(\"scores.txt\", \"w\") as file:\n    for line in lines:\n        file.write(line + \"\\n\")\n\ntotal = 0\nwith open(\"scores.txt\", \"r\") as file:\n    for line in file:\n        total = total + int(line.strip())\nprint(total)",
      "worked_example_note": "strip() removes each line's trailing newline before int() converts it to a number - without strip(), int() would raise an error.",
      "worked_example_demo_input": ["10", "20", "30"]
    },
    {
      "slug": "gcse-files-3",
      "title": "Exam-style file problems",
      "summary": "Build a file, then search or aggregate its contents.",
      "notes": "Most file questions have the same two-stage shape:\n\n1. **Build** — read data from the user and save it to a file\n2. **Process** — reopen the file and search, count, or total what's in it\n\n---\n\nThree mistakes cost the most marks in OCR file questions:\n\n- forgetting to `.close()` a file (or not using `with`)\n- forgetting to `.strip()` a line before comparing it or converting it to a number\n- writing several pieces of text without a `\\n` between them, so they all run together on one line",
      "worked_example": "names = [input() for _ in range(3)]\nwith open(\"register.txt\", \"w\") as file:\n    for name in names:\n        file.write(name + \"\\n\")\n\ntarget = input()\nfound = False\nwith open(\"register.txt\", \"r\") as file:\n    for line in file:\n        if line.strip() == target:\n            found = True\nprint(\"Found\" if found else \"Not found\")",
      "worked_example_note": "Comparing line.strip() to target matters here - without strip(), the trailing newline on every line read back from the file would make the comparison fail even for a genuine match.",
      "worked_example_demo_input": ["ada", "alan", "grace", "alan"]
    }
  ],
  "quiz": [
    { "lessonSlug": "gcse-files-1", "question": "Which file mode opens a file for writing, replacing any existing content?", "options": ["\"w\"", "\"r\"", "\"a\"", "\"x\""], "answer": "\"w\"", "explanation": "\"w\" (write) mode creates the file if needed and empties it if it already exists, ready for new content." },
    { "lessonSlug": "gcse-files-1", "question": "Which file mode adds new content to the end of a file, keeping what's already there?", "options": ["\"a\"", "\"w\"", "\"r\"", "\"c\""], "answer": "\"a\"", "explanation": "\"a\" (append) mode writes new content after whatever is already in the file, instead of replacing it." },
    { "lessonSlug": "gcse-files-1", "question": "Why must you call .close() on a file after writing to it?", "options": ["To make sure everything written is actually saved", "To delete the file", "To make the file read-only", "It isn't necessary"], "answer": "To make sure everything written is actually saved", "explanation": "Closing a file ensures anything written to it is properly saved, rather than staying in a temporary buffer." },
    { "lessonSlug": "gcse-files-2", "question": "A line read from a file usually has an extra character at the end that isn't there when you type it directly. What is it, and what removes it?", "options": ["A newline character, removed with .strip()", "A space, removed with .upper()", "A comma, removed with .split()", "Nothing needs removing"], "answer": "A newline character, removed with .strip()", "explanation": "Each line read from a file keeps its trailing newline (\\n) - strip() removes it, which matters before comparing text or converting it to a number." },
    { "lessonSlug": "gcse-files-2", "question": "What does `with open(\"data.txt\") as file:` do differently from a plain open() call?", "options": ["It closes the file automatically once the block finishes", "It opens the file faster", "It only works for reading, never writing", "It automatically converts the contents to numbers"], "answer": "It closes the file automatically once the block finishes", "explanation": "A with block closes the file for you as soon as the indented block ends, even if an error happens partway through." },
    { "lessonSlug": "gcse-files-2", "question": "Why would int(line) raise an error on a line read from a file, even though the line clearly contains just a number?", "options": ["The line still has a trailing newline character attached", "int() doesn't work on file data", "The file must be opened in \"a\" mode first", "It wouldn't raise an error"], "answer": "The line still has a trailing newline character attached", "explanation": "Without stripping it first, the leftover newline character makes the text not a clean number, and int() raises an error." },
    { "lessonSlug": "gcse-files-3", "question": "In the search example, why does the code compare line.strip() to the target instead of comparing line directly?", "options": ["Without strip(), the trailing newline on every line would make the comparison always fail", "strip() converts the line to a number", "line.strip() is faster to compare", "There's no difference between the two"], "answer": "Without strip(), the trailing newline on every line would make the comparison always fail", "explanation": "Every line read from a file keeps its newline character, so comparing it unstripped to a target with no newline would never match, even for identical text." },
    { "lessonSlug": "gcse-files-3", "question": "What is the risk of forgetting to add \\n when writing several pieces of text to a file with .write()?", "options": ["They all end up joined together on one line", "The file won't save at all", "Python raises an error immediately", "Nothing - .write() adds a newline automatically"], "answer": "They all end up joined together on one line", "explanation": ".write() does not add a newline by itself, so without one, everything you write runs together with no line breaks." },
    { "lessonSlug": "gcse-files-3", "question": "What happens to a file's existing content if you open it in \"w\" mode a second time, after already writing to it once?", "options": ["It's erased - \"w\" mode always starts the file empty", "It's added to", "It's left completely unchanged", "The file is deleted entirely from disk"], "answer": "It's erased - \"w\" mode always starts the file empty", "explanation": "\"w\" mode empties the file every time it's opened that way, which is why appending needs \"a\" mode instead." }
  ],
  "tasks": [
    { "slug": "gcse-files-t1-01", "lesson": 1, "tier": 1, "difficulty": 1, "xp": 10,
      "title": "Write to a file",
      "brief": "Input a line of text. Write it to a file called `notes.txt`. Read the file back and print its contents.",
      "starter": "", "hints": ["Write and close the file first, then separately open it again to read it.", "Open with mode \"w\" to write, close it, then open the same filename again with mode \"r\" to read it back."],
      "tests": [{ "stdin": "remember this", "expect": "remember this" }] },
    { "slug": "gcse-files-t1-02", "lesson": 1, "tier": 1, "difficulty": 2, "xp": 15,
      "title": "Append, don't overwrite",
      "brief": "Input two lines of text. Write the first line to a file called `log.txt` in write mode. Then reopen the file in append mode and write the second line, with a newline between the two. Read the whole file back and print it.",
      "starter": "", "hints": ["\"w\" for the first write, \"a\" for the second - appending keeps the first line instead of erasing it.", "Open with \"w\" for the very first write, then \"a\" for any write after that, so earlier lines aren't erased."],
      "tests": [{ "stdin": "first\nsecond", "expect": "first\nsecond" }] },
    { "slug": "gcse-files-t1-03", "lesson": 1, "tier": 1, "difficulty": 2, "xp": 15,
      "title": "Overwrite check",
      "brief": "Input two lines of text. Write the first line to a file called `data.txt`. Then write the second line to `data.txt` again, still in write mode (not append) — this replaces the first line entirely. Read the file back and print its contents.\n\nThis demonstrates that \"w\" mode empties the file every time it's opened, so only the second line should remain.",
      "starter": "", "hints": ["Both writes use \"w\" mode - the second one starts the file empty again before writing, so the first line is gone.", "Opening the same file with \"w\" mode a second time empties it before the new write happens."],
      "tests": [{ "stdin": "first\nsecond", "expect": "second" }] },
    { "slug": "gcse-files-t1-04", "lesson": 1, "tier": 1, "difficulty": 2, "xp": 15,
      "title": "Read line by line",
      "brief": "Input 3 lines of text. Write each one to its own line in a file called `poem.txt` (remember to add \\n after each one). Reopen the file and use .readlines() to read it back as a list. Print how many lines that list contains.",
      "starter": "", "hints": [".readlines() gives back a list with one entry per line - len() on that list gives the line count.", "Write each line with a newline character at the end, then reopen the file and call .readlines() to get a list you can measure with len()."],
      "tests": [{ "stdin": "a\nb\nc", "expect": "3" }] },
    { "slug": "gcse-files-t1-stretch", "lesson": 1, "tier": 4, "difficulty": 5, "xp": 40, "stretch": true,
      "title": "Round-trip character count",
      "brief": "Read a line of text. Write it to a file called `msg.txt`. Reopen the file and read its full contents back into a variable. Print the number of characters in what was read back from the file (not the length of the original input variable — actually count what came back from the round trip).\n\nFor example, hi there has 8 characters.",
      "starter": "", "hints": ["Write and close, then reopen, read the whole file into a new variable, and measure that variable's length with len() — don't reuse len() on the original input.", "Reopen the file and read its contents into a new variable — measure that variable's length, not the one you originally wrote."],
      "tests": [{ "stdin": "hello", "expect": "5" }, { "stdin": "hi there", "expect": "8" }] },
    { "slug": "gcse-files-t2-01", "lesson": 2, "tier": 2, "difficulty": 2, "xp": 15,
      "title": "Save and total",
      "brief": "Input 3 whole numbers, one per line. Write each one to a file called `scores.txt`, one per line. Reopen the file, read each line, and print the total of all three numbers.",
      "starter": "", "hints": ["Remember to strip() each line before converting it to a number with int() — it still has a trailing newline attached.", "Write each number as text with a newline after it, then reopen the file and loop over its lines, stripping and casting each one before adding it to a running total."],
      "tests": [{ "stdin": "1\n2\n3", "expect": "6" }] },
    { "slug": "gcse-files-t2-02", "lesson": 2, "tier": 2, "difficulty": 2, "xp": 15,
      "title": "Count lines with with",
      "brief": "Input 4 lines of text. Save them to a file called `items.txt`, one per line, using a with block. Reopen the file, also using a with block, and print how many lines it contains.",
      "starter": "", "hints": ["Writing a list to a file needs one write per item, usually with a newline character added after each one.", "Reopen the file after writing and closing it, then use .readlines() to count how many lines it has."],
      "tests": [{ "stdin": "a\nb\nc\nd", "expect": "4" }] },
    { "slug": "gcse-files-t2-03", "lesson": 2, "tier": 2, "difficulty": 3, "xp": 20,
      "title": "Strip before comparing",
      "brief": "Input 3 whole numbers, one per line. Write each to a file called `marks.txt`, one per line. Reopen the file. Count and print how many of the saved marks are 50 or more — remember to strip() each line and convert it to a number before comparing it.",
      "starter": "", "hints": ["The comparison has to happen after both strip() and int() are applied to each line, not before.", "Loop over the reopened file's lines, stripping and casting each one to a number before comparing it against the pass mark."],
      "tests": [{ "stdin": "40\n60\n55", "expect": "2" }] },
    { "slug": "gcse-files-t2-04", "lesson": 2, "tier": 2, "difficulty": 3, "xp": 20,
      "title": "Read a specific line",
      "brief": "Input 5 words, one per line. Save them to a file called `words.txt`, one per line. Reopen the file, read all the lines into a list, and print the 3rd word that was saved (counting from 1).",
      "starter": "", "hints": ["Position 3 (counting from 1) is index 2 in the list. Remember to strip() the line before printing it, to drop its trailing newline.", "Read all the lines into a list, then index into that list at the position you need, stripping the trailing newline before printing."],
      "tests": [{ "stdin": "a\nb\nc\nd\ne", "expect": "c" }] },
    { "slug": "gcse-files-t2-stretch", "lesson": 2, "tier": 4, "difficulty": 5, "xp": 40, "stretch": true,
      "title": "Average from a file",
      "brief": "Read 4 decimal numbers, one per line. Write them to a file called `readings.txt`, one per line, using a with block. Reopen the file, also with a with block, and calculate the average of the saved numbers, rounded to 2 decimal places. Print the average.",
      "starter": "", "hints": ["Track a running total and a count as you loop through the reopened file's lines, then divide the two once the loop finishes.", "Cast and strip each line as you add it to the running total, then round the division once the loop finishes."],
      "tests": [{ "stdin": "1.0\n2.0\n3.0\n4.0", "expect": "2.5" }, { "stdin": "10\n20\n30\n40", "expect": "25.0" }] },
    { "slug": "gcse-files-t3-01", "lesson": 3, "tier": 3, "difficulty": 3, "xp": 25,
      "title": "Search a saved file",
      "brief": "Read 4 names, one per line. Save them to a file called `register.txt`, one per line. Read a fifth line: a name to search for. Print `Found` if that name is in the file, otherwise print `Not found`.",
      "starter": "", "hints": ["Compare line.strip() to the search name inside the loop, not line itself, to avoid the trailing newline breaking the match.", "Set a Boolean flag to False before the loop, then set it to True inside the loop once a stripped line matches the target."],
      "tests": [{ "stdin": "Ada\nAlan\nGrace\nLin\nGrace", "expect": "Found" }, { "stdin": "Ada\nAlan\nGrace\nLin\nBob", "expect": "Not found" }] },
    { "slug": "gcse-files-t3-02", "lesson": 3, "tier": 3, "difficulty": 3, "xp": 25,
      "title": "Highest saved score",
      "brief": "Read 5 whole numbers, one per line. Save them to a file called `scores.txt`, one per line. Reopen the file and print the highest number saved.",
      "starter": "", "hints": ["Track a highest-so-far as you loop through the reopened file, the same pattern as tracking a highest value from direct input.", "Cast and strip each line as you read it, then compare it against your running highest-so-far, updating it when a bigger value turns up."],
      "tests": [{ "stdin": "3\n9\n1\n7\n5", "expect": "9" }] },
    { "slug": "gcse-files-t3-03", "lesson": 3, "tier": 3, "difficulty": 4, "xp": 30,
      "title": "Count matching lines",
      "brief": "Read a whole number N, then N words, one per line. Save the words to a file called `words.txt`, one per line. Reopen the file and print how many of the saved words are longer than 4 characters.\n\nFor example, cat, elephant, dog, butterfly has two words longer than 4 characters (elephant and butterfly).",
      "starter": "", "hints": ["Count using the length of each stripped line, compared against the threshold given in the brief.", "Loop over the reopened file's lines, stripping each one and comparing its length before deciding whether to count it."],
      "tests": [{ "stdin": "4\ncat\nelephant\ndog\nbutterfly", "expect": "2" }] },
    { "slug": "gcse-files-t4-01", "lesson": 3, "tier": 4, "difficulty": 4, "xp": 35,
      "title": "Class register report",
      "brief": "A teacher saves a class register with 4 students. For each student, two lines are entered: their name, then Y or N for whether they're present.\n\nWrite a program that:\n- reads all 4 name/attendance pairs (8 lines total)\n- saves all 8 lines to a file called register.txt, one per line\n- reopens the file and counts how many students are marked present (Y)\n- prints the result in the format `Present: <count>`\n\nFor example, if 3 of the 4 students are marked Y, print `Present: 3`.",
      "starter": "", "hints": ["Every other line in the file is an attendance mark, not a name — you only need to check the Y/N lines when counting.", "Loop over the reopened file's lines, only counting the ones that match the attendance mark you're looking for."],
      "tests": [{ "stdin": "Ada\nY\nAlan\nN\nGrace\nY\nLin\nY", "expect": "Present: 3" }] },
    { "slug": "gcse-files-t4-02", "lesson": 3, "tier": 4, "difficulty": 5, "xp": 40,
      "title": "Word frequency in a saved file",
      "brief": "Read a whole number N, then N words, one per line, then one more line: a target word.\n\nSave the N words to a file called words.txt, one per line. Reopen the file and count how many times the target word appears in it (an exact match). Print the count in the format `Count: <n>`.",
      "starter": "", "hints": ["Count matches inside the reopened-file loop, comparing each stripped line to the target.", "Loop over the reopened file's lines, stripping each one and comparing it to the target before counting a match."],
      "tests": [{ "stdin": "5\ncat\ndog\ncat\nbird\ncat\ncat", "expect": "Count: 3" }] },
    { "slug": "gcse-files-t3-stretch", "lesson": 3, "tier": 4, "difficulty": 5, "xp": 40, "stretch": true,
      "title": "Merge two files",
      "brief": "Read 6 whole numbers, one per line: the first 3 go into a file called `file1.txt` (one per line), the next 3 go into a file called `file2.txt` (one per line).\n\nCombine the contents of both files into a new third file called `combined.txt` (every line from `file1.txt`, followed by every line from `file2.txt`).\n\nReopen `combined.txt`, total every number in it, and print the total.",
      "starter": "", "hints": ["Write file1.txt and file2.txt first and close them. Then open combined.txt for writing, and copy each line across from file1.txt and then file2.txt into it, before finally reopening combined.txt to total it.", "Open the destination file for writing, then open each source file in turn and copy its lines across before moving to the next one."],
      "tests": [{ "stdin": "1\n2\n3\n4\n5\n6", "expect": "21" }, { "stdin": "10\n20\n30\n5\n5\n5", "expect": "75" }] }
  ],
  "homeworkTasks": [
    { "slug": "gcse-files-hw-01", "tier": 1, "difficulty": 1, "xp": 10, "title": "Write and read your name",
      "brief": "Input a name. Write it to a file called `name.txt`. Reopen the file and print what was written.",
      "starter": "", "hints": ["Write and close the file first, then separately open it again to read it.", "Open with mode \"w\" to write, close it, then open the same filename again with mode \"r\" to read it back."],
      "tests": [{ "stdin": "Ada", "expect": "Ada" }, { "stdin": "Sam", "expect": "Sam" }] },
    { "slug": "gcse-files-hw-02", "tier": 1, "difficulty": 1, "xp": 15, "title": "Two diary lines",
      "brief": "Input two activities, one at a time. Write them to `diary.txt`, one per line. Reopen the file and print both lines back.",
      "starter": "", "hints": ["Add a newline character after each line written, so they don't run together.", "Write both lines with \"\\n\" after each, close, then reopen and read the whole file back."],
      "tests": [{ "stdin": "Reading\nSwimming", "expect": "Reading\nSwimming" }, { "stdin": "Running\nCooking", "expect": "Running\nCooking" }] },
    { "slug": "gcse-files-hw-03", "tier": 2, "difficulty": 2, "xp": 20, "title": "Add a task",
      "brief": "Input a first task. Write it to `todo.txt`. Input a second task. Append it to the same file, so both are kept. Reopen the file and print both lines.",
      "starter": "", "hints": ["\"w\" for the first write, \"a\" for the second — appending keeps the first line instead of erasing it.", "Write the first task with \"w\" mode, close it, then reopen with \"a\" mode for the second task."],
      "tests": [{ "stdin": "Buy milk\nWalk dog", "expect": "Buy milk\nWalk dog" }, { "stdin": "Email teacher\nTidy room", "expect": "Email teacher\nTidy room" }] },
    { "slug": "gcse-files-hw-04", "tier": 1, "difficulty": 1, "xp": 10, "title": "Count shopping items",
      "brief": "Input a whole number N. Input N shopping items, one at a time. Write them all to `shopping.txt`, one per line. Reopen the file and print how many lines it has.",
      "starter": "", "hints": [".readlines() gives back a list with one entry per line — len() on that list gives the line count.", "Write each item with a newline after it, then reopen and call .readlines() to count them."],
      "tests": [{ "stdin": "3\nApples\nBread\nMilk", "expect": "3" }, { "stdin": "2\nEggs\nButter", "expect": "2" }] },
    { "slug": "gcse-files-hw-05", "tier": 2, "difficulty": 2, "xp": 20, "title": "Double a stored price",
      "brief": "Input a price, which may include pence. Write it to `prices.txt`. Reopen the file, read it back, convert it to a float, and print it doubled.",
      "starter": "", "hints": ["Reopen the file and cast the line read back before doing arithmetic with it.", "Read the line back, strip() it, cast with float(), then multiply by 2 and print."],
      "tests": [{ "stdin": "4.50", "expect": "9.0" }, { "stdin": "10.00", "expect": "20.0" }] },
    { "slug": "gcse-files-hw-06", "tier": 2, "difficulty": 2, "xp": 20, "title": "Largest number in a file",
      "brief": "Input a whole number N. Input N whole numbers, one at a time. Write them to `numbers.txt`, one per line. Reopen the file and print the largest one.",
      "starter": "", "hints": ["Track a highest-so-far as you loop through the reopened file's lines, casting each one.", "Strip and cast each line as you read it, comparing it against a running highest value."],
      "tests": [{ "stdin": "4\n3\n9\n2\n7", "expect": "9" }, { "stdin": "3\n1\n5\n2", "expect": "5" }] },
    { "slug": "gcse-files-hw-07", "tier": 2, "difficulty": 2, "xp": 20, "title": "Smallest number in a file",
      "brief": "Input a whole number N. Input N whole numbers, one at a time. Write them to `values.txt`, one per line. Reopen the file and print the smallest one.",
      "starter": "", "hints": ["Track a lowest-so-far as you loop through the reopened file's lines, casting each one.", "Strip and cast each line as you read it, comparing it against a running lowest value."],
      "tests": [{ "stdin": "4\n3\n9\n2\n7", "expect": "2" }, { "stdin": "3\n5\n5\n5", "expect": "5" }] },
    { "slug": "gcse-files-hw-08", "tier": 3, "difficulty": 3, "xp": 25, "title": "Words containing a letter",
      "brief": "Input a whole number N. Input N words, one at a time. Write them to `vocabulary.txt`, one per line. Input a single letter. Reopen the file and print how many words contain that letter.",
      "starter": "", "hints": ["Use in to check each stripped line against the target letter.", "Loop over the reopened file's lines, stripping each one and checking whether the letter is in it."],
      "tests": [{ "stdin": "3\napple\nbanana\ncherry\na", "expect": "2" }, { "stdin": "2\ncat\ndog\no", "expect": "1" }] },
    { "slug": "gcse-files-hw-09", "tier": 2, "difficulty": 3, "xp": 25, "title": "Words with a prefix",
      "brief": "Input a whole number N. Input N words, one at a time. Write them to `log.txt`, one per line. Input a prefix. Reopen the file and print how many words start with that prefix.",
      "starter": "", "hints": ["One string method checks what a piece of text begins with.", "Loop over the reopened file's lines, stripping each one and calling .startswith() on it."],
      "tests": [{ "stdin": "3\napple\napricot\nbanana\nap", "expect": "2" }, { "stdin": "2\ncat\ndog\nc", "expect": "1" }] },
    { "slug": "gcse-files-hw-10", "tier": 3, "difficulty": 3, "xp": 25, "title": "Names from records",
      "brief": "Input a whole number N. Input N pairs of name and age, one after another. Write each pair to `people.txt` as one line in the format `name,age` (joined with a comma, no space). Reopen the file and print just the names, each on its own line.",
      "starter": "", "hints": ["Splitting each stripped line on the comma gives a list of two pieces — the first is the name.", "Loop over the reopened file's lines, stripping and splitting each one on \",\", printing the first piece."],
      "tests": [{ "stdin": "2\nAda\n30\nSam\n25", "expect": "Ada\nSam" }, { "stdin": "1\nMax\n40", "expect": "Max" }] },
    { "slug": "gcse-files-hw-11", "tier": 2, "difficulty": 2, "xp": 20, "title": "Search for a result",
      "brief": "Input a whole number N. Input N whole number results, one at a time. Write them to `results.txt`, one per line. Input a target score. Reopen the file and print `Found` if it appears in the file, otherwise print `Not found`.",
      "starter": "", "hints": ["Compare each stripped, cast line to the target inside the loop.", "Loop over the reopened file's lines, checking for a match and setting a Boolean flag when one is found."],
      "tests": [{ "stdin": "3\n50\n60\n70\n60", "expect": "Found" }, { "stdin": "3\n50\n60\n70\n99", "expect": "Not found" }] },
    { "slug": "gcse-files-hw-12", "tier": 3, "difficulty": 3, "xp": 25, "title": "Total characters in a file",
      "brief": "Input a whole number N. Input N words, one at a time. Write them to `text.txt`, one per line. Reopen the file and print the total number of characters across all the words combined (not counting the newlines).",
      "starter": "", "hints": ["len() on each stripped line gives that line's character count.", "Loop over the reopened file's lines, stripping each one and adding its len() to a running total."],
      "tests": [{ "stdin": "3\ncat\ndog\nbird", "expect": "10" }, { "stdin": "2\nhi\nbye", "expect": "5" }] },
    { "slug": "gcse-files-hw-13", "tier": 2, "difficulty": 2, "xp": 20, "title": "Total stock from a file",
      "brief": "Input a whole number N. Input N whole number stock counts, one at a time. Write them to `inventory.txt`, one per line. Reopen the file and print the total stock.",
      "starter": "", "hints": ["Strip and cast each line before adding it to a running total.", "Loop over the reopened file's lines, casting each one and adding it to a total."],
      "tests": [{ "stdin": "3\n10\n5\n8", "expect": "23" }, { "stdin": "2\n0\n7", "expect": "7" }] },
    { "slug": "gcse-files-hw-14", "tier": 2, "difficulty": 2, "xp": 20, "title": "Copy a file",
      "brief": "Input a line of text. Write it to `source.txt`. Reopen `source.txt` and copy its contents into a new file called `backup.txt`. Reopen `backup.txt` and print its contents, to confirm the copy worked.",
      "starter": "", "hints": ["Reading the whole source file into a variable lets you write that same variable straight into the new file.", "Read source.txt's content into a variable, write that variable into backup.txt, then reopen backup.txt to print it."],
      "tests": [{ "stdin": "Hello there", "expect": "Hello there" }, { "stdin": "Testing 123", "expect": "Testing 123" }] },
    { "slug": "gcse-files-hw-15", "tier": 2, "difficulty": 2, "xp": 20, "title": "Count freezing readings",
      "brief": "Input a whole number N. Input N whole number temperatures, one at a time. Write them to `temperatures.txt`, one per line. Reopen the file and print how many are below freezing (below 0).",
      "starter": "", "hints": ["Strip and cast each line before comparing it to 0.", "Loop over the reopened file's lines, counting how many cast values are below 0."],
      "tests": [{ "stdin": "4\n-2\n5\n-8\n3", "expect": "2" }, { "stdin": "3\n1\n2\n3", "expect": "0" }] },
    { "slug": "gcse-files-hw-16", "tier": 2, "difficulty": 2, "xp": 20, "title": "Reversed note",
      "brief": "Input a sentence. Write it to `notes.txt`. Reopen the file, read the line back, and print it reversed.",
      "starter": "", "hints": ["A slice with a step of -1 reads a string backwards.", "Strip the line read back, then slice it with [::-1] before printing."],
      "tests": [{ "stdin": "hello", "expect": "olleh" }, { "stdin": "cat", "expect": "tac" }] },
    { "slug": "gcse-files-hw-17", "tier": 2, "difficulty": 2, "xp": 20, "title": "Count exam passes",
      "brief": "Input a whole number N. Input N whole number exam scores, one at a time. Write them to `exam_scores.txt`, one per line. Reopen the file and print how many students passed (scored 50 or more).",
      "starter": "", "hints": ["Strip and cast each line before comparing it against 50.", "Loop over the reopened file's lines, counting how many cast values are 50 or more."],
      "tests": [{ "stdin": "4\n85\n40\n50\n60", "expect": "3" }, { "stdin": "3\n10\n20\n30", "expect": "0" }] },
    { "slug": "gcse-files-hw-18", "tier": 3, "difficulty": 3, "xp": 25, "title": "The longest line",
      "brief": "Input a whole number N. Input N pieces of feedback text, one at a time. Write them to `feedback.txt`, one per line. Reopen the file and print the longest line (the one with the most characters, once stripped).",
      "starter": "", "hints": ["Track a longest-so-far as you loop through the reopened file's lines, comparing lengths.", "Strip each line, compare its len() against a running longest, and keep the line itself, not just its length."],
      "tests": [{ "stdin": "3\nGood\nExcellent work\nOK", "expect": "Excellent work" }, { "stdin": "2\nHi\nHello there friend", "expect": "Hello there friend" }] },
    { "slug": "gcse-files-hw-19", "tier": 2, "difficulty": 2, "xp": 20, "title": "Search inside a file",
      "brief": "Input a sentence. Write it to `message.txt`. Input a word to search for. Reopen the file, read its whole contents, and print `True` if the word appears anywhere in it, otherwise print `False`.",
      "starter": "", "hints": ["The in keyword can check for a substring anywhere inside a whole piece of text, not just a single line.", "Read the whole file into one variable with .read(), then use in to check for the word."],
      "tests": [{ "stdin": "the quick brown fox\nquick", "expect": "True" }, { "stdin": "the quick brown fox\nslow", "expect": "False" }] },
    { "slug": "gcse-files-hw-20", "tier": 2, "difficulty": 2, "xp": 20, "title": "Total cost from a file",
      "brief": "Input a whole number N. Input N costs, which may include pence, one at a time. Write them to `costs.txt`, one per line. Reopen the file and print the total, rounded to 2 decimal places.",
      "starter": "", "hints": ["Strip and cast each line to a float before adding it to a running total.", "Loop over the reopened file's lines, casting and adding each one, then round(total, 2) before printing."],
      "tests": [{ "stdin": "3\n1.50\n2.25\n0.99", "expect": "4.74" }, { "stdin": "2\n5.00\n5.00", "expect": "10.0" }] },
    { "slug": "gcse-files-hw-21", "tier": 2, "difficulty": 2, "xp": 20, "title": "Sorted numbers from a file",
      "brief": "Input a whole number N. Input N whole numbers, one at a time. Write them to `raw.txt`, one per line. Reopen the file and print them sorted from smallest to largest, each on its own line.",
      "starter": "", "hints": ["Casting each line into a list first lets you sort the whole list at once.", "Build a list from the reopened file's stripped, cast lines, then loop over sorted(list) to print them."],
      "tests": [{ "stdin": "4\n9\n2\n7\n3", "expect": "2\n3\n7\n9" }, { "stdin": "3\n5\n1\n3", "expect": "1\n3\n5" }] },
    { "slug": "gcse-files-hw-22", "tier": 1, "difficulty": 1, "xp": 10, "title": "Count the class list",
      "brief": "Input a whole number N. Input N names, one at a time. Write them to `class_list.txt`, one per line. Reopen the file and print how many names it contains.",
      "starter": "", "hints": [".readlines() gives back a list with one entry per line.", "Write each name with a newline after it, then reopen and call .readlines() to count them."],
      "tests": [{ "stdin": "3\nAda\nSam\nMax", "expect": "3" }, { "stdin": "2\nA\nB", "expect": "2" }] },
    { "slug": "gcse-files-hw-23", "tier": 2, "difficulty": 2, "xp": 20, "title": "Count active accounts",
      "brief": "Input a whole number N. Input N account statuses, one at a time, each either Active or Inactive. Write them to `accounts.txt`, one per line. Reopen the file and print how many are exactly Active.",
      "starter": "", "hints": ["Compare each stripped line against the exact word Active.", "Loop over the reopened file's lines, counting how many stripped lines equal \"Active\"."],
      "tests": [{ "stdin": "3\nActive\nInactive\nActive", "expect": "2" }, { "stdin": "2\nInactive\nInactive", "expect": "0" }] },
    { "slug": "gcse-files-hw-24", "tier": 2, "difficulty": 2, "xp": 20, "title": "Total tickets booked",
      "brief": "Input a whole number N. Input N whole number ticket counts, one at a time. Write them to `bookings.txt`, one per line. Reopen the file and print the total number of tickets booked.",
      "starter": "", "hints": ["Strip and cast each line before adding it to a running total.", "Loop over the reopened file's lines, casting each one and adding it to a total."],
      "tests": [{ "stdin": "3\n2\n5\n1", "expect": "8" }, { "stdin": "2\n10\n10", "expect": "20" }] },
    { "slug": "gcse-files-hw-25", "tier": 3, "difficulty": 3, "xp": 25, "title": "Comparing two files",
      "brief": "Input a whole number N. Input N lines of text, one at a time. Write them to `file_a.txt`. Input a whole number M. Input M lines of text, one at a time. Write them to `file_b.txt`. Reopen both files and print `A has more lines`, `B has more lines`, or `Equal lines`, depending on which file has more lines.",
      "starter": "", "hints": ["Reopen both files and use .readlines() on each to compare their lengths.", "Count the lines in each reopened file, then compare the two counts with an if/elif/else."],
      "tests": [{ "stdin": "3\nx\ny\nz\n2\np\nq", "expect": "A has more lines" }, { "stdin": "1\nx\n1\ny", "expect": "Equal lines" }] }
  ],
  "practiceTasks": [
    { "slug": "gcse-files-p-01", "tier": 1, "difficulty": 1, "xp": 15, "title": "Save a high score",
      "brief": "Input a player's name and their score. Write a single line to `highscore.txt` in the form `<name>: <score>`. Reopen the file and print its contents.",
      "starter": "", "hints": ["Write the combined line with a file's .write() method, then close the file before reopening it to read.", "with open(\"highscore.txt\", \"w\") as f: f.write(f\"{name}: {score}\") - then reopen with \"r\" and print(f.read())."],
      "tests": [{ "stdin": "Ada\n95", "expect": "Ada: 95" }] },
    { "slug": "gcse-files-p-02", "tier": 2, "difficulty": 2, "xp": 20, "title": "Append a log entry",
      "brief": "Input a message. Open `log.txt` in append mode and add the message as a new line. Reopen the file and print its contents.",
      "starter": "", "hints": ["Append mode is \"a\" instead of \"w\" - it adds to the end of the file rather than overwriting it.", "with open(\"log.txt\", \"a\") as f: f.write(message + \"\\n\") - then reopen with \"r\" and print(f.read().strip())."],
      "tests": [{ "stdin": "Server started", "expect": "Server started" }] },
    { "slug": "gcse-files-p-03", "tier": 2, "difficulty": 2, "xp": 20, "title": "Count lines",
      "brief": "Input a whole number N. Input N lines of text, one at a time. Write them to `notes.txt`, one per line. Reopen the file and print how many lines it contains.",
      "starter": "", "hints": ["Write each line with a newline after it, then use .readlines() on the reopened file to count them.", "for _ in range(n): f.write(input() + \"\\n\") - then reopen and print(len(f.readlines()))."],
      "tests": [{ "stdin": "3\nfirst\nsecond\nthird", "expect": "3" }] },
    { "slug": "gcse-files-p-04", "tier": 3, "difficulty": 3, "xp": 30, "title": "Average from a file",
      "brief": "Input a whole number N. Input N whole number readings, one at a time. Write them to `readings.txt`, one per line. Reopen the file and print the mean of the readings, rounded to 2 decimal places.",
      "starter": "", "hints": ["Cast each line back to a number after reopening the file, before averaging them.", "Reopen, cast each stripped line to int, sum them, divide by len(), then round(total / count, 2)."],
      "tests": [{ "stdin": "3\n40\n55\n45", "expect": "46.67" }] },
    { "slug": "gcse-files-p-05", "tier": 3, "difficulty": 3, "xp": 30, "title": "Guest list check-in",
      "brief": "Input a whole number N. Input N guest names, one at a time. Write them to `guests.txt`, one per line. Input a name to check in. Reopen the file and print `Found` if that name is in the list, otherwise print `Not found`.",
      "starter": "", "hints": ["Strip each reopened line before comparing it, since it will still have a trailing newline.", "Reopen the file, build a list of stripped lines, then check if the name to find is in that list."],
      "tests": [{ "stdin": "3\nAda\nAlan\nGrace\nAlan", "expect": "Found" }, { "stdin": "3\nAda\nAlan\nGrace\nBob", "expect": "Not found" }] }
  ]
}
